// import { Toast, ToastContainer } from 'react-bootstrap';
// import { useCart } from '../context/CartContext';

// export default function ToastNotifDelete() {
//   const { showToast, setShowToast, toastMsg } = useCart();

//   return (
//     <ToastContainer position="bottom-end" className="p-2">
//       <Toast bg="failure" onClose={() => setShowToast(false)} show={showToast} delay={2000} autohide>
//         <Toast.Body className="text-white">{toastMsg}</Toast.Body>
//       </Toast>
//     </ToastContainer>
//   );
// }
